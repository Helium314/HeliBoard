<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
  <!-- when using inset drawable, insets are only respected in preview -->
  <!-- when setting width/height for the whole drawable (and not just a layer), it's ignored too -->
  <item
      android:width="24dp"
      android:height="24dp"
      android:drawable="@drawable/ic_emoji_activities"
      />
</layer-list>